/* eslint-disable */
import { API } from 'aws-amplify';

const state = {
};
const getters = {
};
const mutations = {
};
const actions = {
  userCreateUser(context) {
    // await API.graphql({
    //   query: createTodo,
    //   variables: {input: todo},
    // });
  }
};

export default {
  state,
  getters,
  mutations,
  actions
};
